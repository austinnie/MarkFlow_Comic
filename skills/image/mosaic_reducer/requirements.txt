# ============================================================
# 基础依赖（所有方案通用）
# ============================================================
Pillow>=9.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# ============================================================
# 方案一：NVIDIA GPU 加速
# ============================================================
# 需要 NVIDIA GPU with Tensor Cores
# 安装命令: pip install nvidia-vfx torch torchvision
torch>=2.0.0
torchvision>=0.15.0
# nvidia-vfx 需要单独从 NVIDIA 官网获取

# ============================================================
# 方案二：通用 CPU（默认，无需额外依赖）
# ============================================================
# 已包含在基础依赖中，无需额外安装


# ============================================================
# 方案三：Jasna 专用工具
# ============================================================
# Jasna 需要单独下载安装，不是 pip 包
# 下载地址: https://github.com/Kruk2/jasna